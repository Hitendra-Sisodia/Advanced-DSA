#include<bits/stdc++.h>
using namespace std;
int ArraySum(int arr[],int n)
{
    if(n < 0){
        return 0;
    }
    else{
        return ArraySum(arr,n-1) + arr[n];
    }
}
int main()
{
    int n = 5;
    int arr[n] = {1,2,3,4,5};
    cout<<ArraySum(arr,n - 1);
}
